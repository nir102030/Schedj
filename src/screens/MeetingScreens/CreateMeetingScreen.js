import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const CreateMeetingScreen = () => {

    return (
        <View>
            <Text>This is the Create meeting screen</Text>
        </View>
    );
};

const style = StyleSheet.create({

});

export default CreateMeetingScreen;